import { ApplicationToken } from './application-token';
import { PageInfo } from '../../common/util/page-info';
export class ApplicationTokenPageInfo extends PageInfo {
    content: ApplicationToken[];
}